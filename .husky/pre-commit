#!/bin/sh
# L1: Unit tests + coverage
# G1: Static analysis (lint-staged + typecheck)
# G2: gitleaks (staged-only scan)
# All run in parallel — see scripts/pre-commit.ts
bun run scripts/pre-commit.ts
